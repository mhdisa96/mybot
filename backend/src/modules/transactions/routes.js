import {Router} from 'express'; import {z} from 'zod'; import {auth,roles} from '../../middleware/auth.js'; import {validate} from '../../middleware/validate.js'; import * as c from './controller.js'; const r=Router(); r.post('/',auth,validate(z.object({productId:z.string().uuid(),destination:z.string().min(5).max(100)})),c.create); r.get('/',auth,roles('SUPER_ADMIN','ADMIN'),c.list); r.get('/:id',auth,c.get); export default r;
